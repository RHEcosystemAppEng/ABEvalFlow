apiVersion: v1
kind: ConfigMap
metadata:
  name: monitoring-state
  namespace: ab-eval-flow
  labels:
    app.kubernetes.io/name: abevalflow
    app.kubernetes.io/component: monitoring
data:
  # This ConfigMap is kept for future use.
  # Quay tag tracking and LiteLLM config hash tracking were removed
  # in APPENG-4911 — those triggers are now event-driven via Quay webhook
  # and GitHub push webhook respectively.
  _placeholder: "unused"
