# MCP Server Configuration
#
# This file defines the MCP servers that the agent will connect to
# during evaluation. Replace the placeholder URL with your actual
# MCP server endpoint.

mcpServers:
  - name: example-server
    url: http://localhost:3000
    # Optional: authentication settings
    # auth:
    #   type: bearer
    #   tokenEnvVar: MCP_SERVER_TOKEN

  # You can define multiple MCP servers if needed
  # - name: another-server
  #   url: http://localhost:3001
